import { Grid } from "@mui/material";
import React from "react";

const MemoByCategory = () => {
  return (
    <Grid sx={{ backgroundColor: "pink", minHeight: "400px" }}>
      MemoByCategory
    </Grid>
  );
};

export default MemoByCategory;
